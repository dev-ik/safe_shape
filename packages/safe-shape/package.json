{
  "name": "safe-shape",
  "version": "1.0.0",
  "description": "Umbrella package for SafeShape runtime contracts and tooling.",
  "type": "module",
  "engines": {
    "node": ">=20.10"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "@safe-shape/cli": "1.0.0",
    "@safe-shape/core": "1.0.0",
    "@safe-shape/http": "1.0.0",
    "@safe-shape/json-schema": "1.0.0",
    "@safe-shape/typescript": "1.0.0",
    "@safe-shape/validation": "1.0.0"
  },
  "scripts": {
    "build": "npm run build --workspace @safe-shape/core && npm run build --workspace @safe-shape/http && npm run build --workspace @safe-shape/json-schema && npm run build --workspace @safe-shape/typescript && npm run build --workspace @safe-shape/validation && npm run build --workspace @safe-shape/cli && tsc -p tsconfig.build.json",
    "typecheck": "npm run build --workspace @safe-shape/core && npm run build --workspace @safe-shape/http && npm run build --workspace @safe-shape/json-schema && npm run build --workspace @safe-shape/typescript && npm run build --workspace @safe-shape/validation && npm run build --workspace @safe-shape/cli && tsc --noEmit",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test .tmp/test/tests/*.test.js"
  }
}
