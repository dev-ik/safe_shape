{
  "name": "@safe-shape/cli",
  "version": "2.0.1",
  "description": "Command-line tools for SafeShape runtime contracts.",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20.10"
  },
  "bin": {
    "safe-shape": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "@safe-shape/compat": "2.0.1",
    "@safe-shape/core": "2.0.1",
    "@safe-shape/json-schema": "2.0.1",
    "@safe-shape/typescript": "2.0.1",
    "@safe-shape/validation": "2.0.1"
  },
  "scripts": {
    "build": "npm run build --workspace @safe-shape/core && npm run build --workspace @safe-shape/compat && npm run build --workspace @safe-shape/json-schema && npm run build --workspace @safe-shape/typescript && npm run build --workspace @safe-shape/validation && tsc -p tsconfig.build.json",
    "typecheck": "npm run build --workspace @safe-shape/core && npm run build --workspace @safe-shape/compat && npm run build --workspace @safe-shape/json-schema && npm run build --workspace @safe-shape/typescript && npm run build --workspace @safe-shape/validation && tsc --noEmit",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test .tmp/test/tests/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dev-ik/safe_shape.git",
    "directory": "packages/cli"
  },
  "homepage": "https://github.com/dev-ik/safe_shape#readme",
  "bugs": {
    "url": "https://github.com/dev-ik/safe_shape/issues"
  }
}
